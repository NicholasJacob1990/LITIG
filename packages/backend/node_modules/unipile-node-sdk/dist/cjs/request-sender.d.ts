import { RequestInput } from './types/request.js';
import { ClientState } from './state.js';
export declare class RequestSender {
    private clientState;
    private defaultHeaders;
    constructor(state: ClientState);
    send<PayloadType>(input: RequestInput): Promise<PayloadType>;
    private buildUrl;
    private validate;
}
