import { ApiVersion, ClientInstantiationOptions, ClientOptions, SupportedProtocols } from './types/index.js';
export declare class ClientState {
    private _baseUrl;
    private _token;
    private _protocol;
    private _domain;
    readonly apiVersion: ApiVersion;
    readonly logRequestResult: boolean;
    readonly logRequestPayload: boolean;
    readonly validateRequestPayload: boolean;
    readonly validateRequestPayloadLevel: ClientOptions['validateRequestPayloadLevel'];
    constructor(baseUrl: string, token: string, options: ClientInstantiationOptions);
    get token(): string;
    get protocol(): SupportedProtocols;
    get domain(): string;
}
